#include <stdio.h>

int main()
{
    // Array Initializing
    int arr[5] = {5, 6, 2, 8, 7};

    for (int i = 0; i < 5; i++)
    {
        printf("%d ", arr[i]);
    }
    // printf("%d %d %d %d %d", arr[0], arr[1], arr[2], arr[3], arr[4]);

    return 0;
}