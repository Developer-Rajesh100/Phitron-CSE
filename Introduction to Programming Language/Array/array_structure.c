#include <stdio.h>

int main()
{
    // Declar
    int num[5];

    // Assign Value in Array index wise
    num[0] = 7;
    num[1] = 3;
    num[2] = 8;
    num[3] = 5;
    num[4] = 9;

    // Print the array value
    printf("The Numbers Are :  %d %d %d %d %d\n", num[0], num[1], num[2], num[3], num[4]);

    return 0;
}